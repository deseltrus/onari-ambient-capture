# Demo scenario (synthetic, prepared)

## The demo user

Daniel, builder in SF, working toward a demo and a team. Running threads
that already live in the seeded history: a signal-pipeline project, a
residency application, and open outreach.

## The unspoken thread: the contact from the talk

The contact is a synthetic ML engineer the user "met at a talk two weeks ago"
(seeded episode). During the live session their LinkedIn profile gets
glanced at, with NO note taken. The consolidation surfaces them anyway,
joined to the seeded thread. The dispatched artifact is an outreach draft
to that contact citing moments the user never typed. That is the whole point,
made visible with one concrete person (name the synthetic contact whatever fits on the day).

## The live wander sequence (the script)

1. Agent session (Codex/Claude window). Hotkey note:
   "interesting, ties into the signal thing"
2. Paper (PDF open). Two notes:
   "section three supports our capture approach"
   "compare this with the pipeline latency issue"
3. Talk on YouTube. One note at a timestamp:
   "that point at minute twelve about context loss, exactly our case"
4. LinkedIn, the contact's profile. NO note. Dwell a few seconds, move on.
5. Back to the agent session; an assistant answer arrived meanwhile
   (delta event, lane 1 or fixture).
6. Pass-through dictation into the agent chat:
   "assemble this and prepare the follow up"

## What consolidation must show

Boards in wander order, notes attached, and the relevance reasoning:
the session's mission (follow up on the signal thread), the seeded
residency/pipeline threads joined, and the contact included WITHOUT a note.
Extremely short, clear, detail-rich.

## The dispatch

Intent frame → policy check → approval (spoken correction if wanted) →
RocketRide pipeline → outreach draft to the contact → result streams back onto
the dispatch surface. Loop closed.
